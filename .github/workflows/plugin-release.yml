name: Plugin release
on:
  push:
    tags:
      - "v*"

permissions:
  contents: write
jobs:
  package:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: "8.3"
      - name: Validate PHP syntax
        run: |
          php -l plugin/src/engine.php
          php -l plugin/src/network.php
          php -l plugin/webui/powerpilot.page
      - name: Test decision engine
        run: |
          set +e
          php plugin/tests/engine_test.php > "$RUNNER_TEMP/engine-test.log" 2>&1
          result=$?
          cat "$RUNNER_TEMP/engine-test.log"
          if [ "$result" -ne 0 ]; then
            echo "### Decision engine test failure" >> "$GITHUB_STEP_SUMMARY"
            cat "$RUNNER_TEMP/engine-test.log" >> "$GITHUB_STEP_SUMMARY"
          fi
          exit "$result"
      - name: Test network tuning
        run: php plugin/tests/network_test.php
      - name: Build Unraid package
        run: |
          TAG_VERSION="$(git describe --tags --abbrev=0 | sed 's/^v//')"
          VERSION="$(cat plugin/VERSION)"
          test "$TAG_VERSION" = "$VERSION"
          bash plugin/build-release.sh "$VERSION"
      - name: Publish release assets
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/powerpilot.plg
            dist/powerpilot-*.txz